---
// interface Props {
//   imageURL: string;
//   backgroundOpacity: number;
// }

// const { imageURL, backgroundOpacity } = Astro.props;
const propsClass = Astro.props.class ?? "";
console.log(propsClass);
---

<div
  class={"flex overflow-hidden relative min-w-8 min-h-8 rounded-3xl p-4 lg:p-6 xl:p-7 " +
    propsClass}
>
  <slot />
</div>
